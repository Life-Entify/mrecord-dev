import React from "react";
import styled from "styled-components";

const Root = styled.div``;

export interface IDepositProps {}

export function Deposits({}: IDepositProps) {
  return <Root> this is deposits</Root>;
}
